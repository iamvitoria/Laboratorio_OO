import java.util.Random;

// Classe DAO para acesso aos dados de Cliente
class ClienteDao {
    // Método para salvar um Cliente no banco de dados
    public void salvarCliente(Cliente cliente) {
        // Código para salvar o Cliente no banco de dados
        // ...
    }

    // Método para buscar um Cliente pelo ID no banco de dados
    public Cliente buscarCliente(int id) {
        // Código para buscar o Cliente no banco de dados
        // ...
        return null; // apenas para exemplificar, substitua pelo código real
    }
}
